import React from "react";

const Input = () => {
     return (
          <div className="inputBox">
               <input type="text" required="required" />
               <span>Tỉnh: </span>
          </div>
     );
};

export default Input;
