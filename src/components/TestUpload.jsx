import React from "react";
import ModalUpload from "./section-components/ModalUpload";
const TestUpload = () => {
     return (
          <>
               <ModalUpload isShow={true} />
          </>
     );
};

export default TestUpload;
